import 'package:flutter/material.dart';

class CleanArquiBase extends StatelessWidget {
  const CleanArquiBase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
